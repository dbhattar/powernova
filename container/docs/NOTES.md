* Asking the LLM to generate few specific statements from the user request and previous context. This should be concise but useful.
* Use the statements generated by the LLM to then search across the documents for more relevant information. This should improve the relevancy of the final response